type: aws_iam

status:
  class: extension
  stability:
    development: [extension]
  distributions: []
  codeowners:
    active: []

# This component is a credentials provider for the config/configcredentials
# framework, not a transport authenticator. It mints short-lived AWS RDS IAM
# database auth tokens and is discovered by receivers via the host extension map.
